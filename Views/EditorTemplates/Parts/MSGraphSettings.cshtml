@model Pelism.MSGraph.Models.MSGraphSettingsPart

<fieldset>
    @Html.LabelFor(m => m.ClientId)
    @Html.TextBoxFor(m => m.ClientId)
</fieldset>
<fieldset>
    @Html.LabelFor(m => m.ClientSecret)
    @Html.TextBoxFor(m => m.ClientSecret)
</fieldset>

